locations:
  - name: "{{ site_name }}"
    status:
      name: "Active"
    location_type:
      name: "Site"
    devices:
      {% for hostname, ip in device_ips.items() %}
      - name: "{{ hostname }}"
        device_type:
          model: "33609"
        role:
          name: "compressor"
        status:
          name: "Active"
        primary_ip4: "!ref:{{ hostname }}_ip"
        interfaces:
          - name: "GigabitEthernet0/0"
            type: "1000base-t"
            status:
              name: "Active"
            ip_addresses:
              - address: "{{ ip }}"
                status:
                  name: "Active"
                "!ref": "{{ hostname }}_ip"
      {% endfor %}
