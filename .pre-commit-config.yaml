repos:
- repo: local
  hooks:
  - id: jobs-python-lint
    name: Lint jobs folder Python files
    entry: python -m pylint
    language: system
    types: [python]
    files: ^jobs/[^/]+\.py$
    exclude: ^jobs/__init__\.py$
    stages: [commit]
